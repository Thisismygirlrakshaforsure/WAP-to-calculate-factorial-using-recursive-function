#include<stdio.h>
#include<conio.h>
    int fact (int);
    void main()
    {
        int f;
        printf("enter a no.");
        scanf("%d", &n);
        fact(i=1; i>=n; i++)
        {
        printf("\n %d", fact(i));
        }
        getch();
    }
        int fact (int i)
        {
            if (i=0)
            {
            return 1;
            }
            else
            {
            return fact (i)*fact (i-1);
        }
    }
